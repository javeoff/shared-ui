import { Meta, Story } from "@storybook/addon-docs";
import { CheckboxExample } from './CheckboxExample'; import {
    themeDecorator
} from "../../../app/ThemeProvider/utils/themeDecorator";

<Meta
  title='UI/Checkbox'
  component={CheckboxExample}
  decorators={[themeDecorator]}
/>

<Story
  name="Checkbox"
  args={{
    label: 'My Label',
  }}
  argTypes={{
    label: {
      name: 'label',
      type: { name: 'string' }
    },
  }}
>
  {CheckboxExample.bind({})}
</Story>
