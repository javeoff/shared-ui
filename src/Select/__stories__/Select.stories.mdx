import { themeDecorator } from "../../ThemeProvider/utils/themeDecorator";
import { Meta, Story } from "@storybook/addon-docs";
import { Select } from '@anagram/ui/Select/Select';
import { SelectExample } from './SelectExample';

<Meta
  title='UI/Select'
  component={Select}
  decorators={[themeDecorator]}
/>

<Story
  name="Select"
  args={{
    label: 'My Label',
    placeholder: 'Choose a variant',
  }}
>
  {SelectExample.bind({})}
</Story>
