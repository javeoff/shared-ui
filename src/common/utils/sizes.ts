import {
  SpaceSize,
  RadiusSize,
  FontSize,
  ScreenSize,
} from '@shared/ui/common/enums';

export const sizes = {
  padding: SpaceSize,
  height: SpaceSize,
  radius: RadiusSize,
  font: FontSize,
  screen: ScreenSize,
};
